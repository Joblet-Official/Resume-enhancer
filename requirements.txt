# Pinned for reproducibility. Versions chosen for stable wheels on
# Windows + macOS + Linux without requiring a CUDA toolchain.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Model + tensor stack. sentence-transformers depends on torch + transformers.
# Render and Vercel run this service on CPU. Force PyTorch's official CPU wheel
# so Linux builds do not include several gigabytes of unused CUDA libraries.
# Use a separate local requirements override if GPU development is ever needed.
--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.1.1
torch==2.9.0+cpu
transformers==4.45.2
numpy==1.26.4
